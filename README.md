# study-group-matcher
StudyCircle — match with compatible study partners via an explainable weighted algorithm (availability, topics, style, goals) + greedy max-min group formation. Vanilla JS, fully unit-tested.
